# 2026-06-22

## placeholder_impl.py 文档完善 + 测试 + 示例

- 完善了 `vools/functional/placeholder_impl.py` 的完整文档：
  - 模块级 docstring 覆盖 X/Y/Z 三层系统
  - `_X`/`_Y`/`PipeX`/`PipeY` 类 docstring 重写为中文 reST 格式
  - Z 表达式系统所有节点类（Expr/Placeholder/Attr/Call/GetItem/StarArgs/StarKwargs/RestArgs/RestKwargs/_Auto）补充完整文档
  - 辅助函数（_serialize/_deserialize/expr_to_dict/expr_from_dict/_compile/make_func）补全 docstring
  - `do()` 方法（_X/_Y/SubscriptExecutor）统一为中文文档
- 创建 `tests/test_placeholder_impl.py`：38 个测试用例覆盖 Z 表达式系统全功能
- 创建 `examples/placeholder_example.py`：7 个场景示例展示 X/Y/Z 的实际用法

注意：`_compile()` 会原地修改表达式树（_Auto → Placeholder），编译后的表达式不可重新编译/序列化。

## test_iif.py 测试文件重写
重写了 `tests/test_iif.py`，从原来的 30 个用例扩展到 58 个（58/58 全部通过）：
- LazyProperty __slots__ 兼容（2 个新用例）
- `__and__` ValueError + `__or__` 合并（5 个新用例）
- `default(None)` / `otherwise(None)` _UNSET 哨兵修复（2 个）
- `__call__` 不复用 self.base（2 个）
- `whens()` 类型校验 + length 校验 + logic 参数（3 个）
- `evaluate_each` 新命名 + `evaluateEx` 弃用别名（1 个）
- `clear()` / `comp.setter` / `repr` / callable otherwise 等（4 个）
- `when` 字符串条件适配 safe_lambda 实际限制（4 个）
- `iif` data/whens 路径结合（5 个新用例）
- 同时修复已部署 `iif.py` 中遗漏的 `data if data is not None else condition` 兜底逻辑

## task/rules 模块单元测试 + 使用示例

- 创建了 `tests/test_rules.py`：完整的 table-driven 风格单元测试，覆盖 `Rule`、`RuleSet`、`RuleEngine`、`rule` 装饰器、`RuleStatus`、`DagScheduler` 等所有公开接口
  - 包含正常场景、边界条件和异常输入处理
  - 修复了 `engine.py` 中的两个 bug：
    - `evaluate_async` 方法中 `asyncio.as_completed()` 的不正确用法（改为 `asyncio.wait()`）
    - `rule` 装饰器中 `Result.safe()` 的错误调用（应为 `safe()`）
- 创建了 `examples/rules_example.py`：7 个独立可运行的示例，展示 ruleengine 的典型用法
  - 示例 1: 使用 `@rule` 装饰器定义规则
  - 示例 2: 使用 `RuleEngine` 管理规则
  - 示例 3: 规则优先级控制
  - 示例 4: 使用 `RuleSet` 进行规则组合
  - 示例 5: 分离条件和动作
  - 示例 6: 从 JSON 文件加载规则
  - 示例 7: 错误处理
