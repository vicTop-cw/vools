# 2026-06-20 工作日志

## do 方法补全任务

继续执行为 vools 项目所有类添加 `do` 实例方法的任务（上一轮 AI 未完成）。

### 完成内容

1. **修复上一轮 AI 的错误**：
   - 移除 dataclass 类中不当添加的 `do` 方法（Action, Task, KeyData, MouseData）
   - 修复 `do` 方法被误设为 `@classmethod` 的问题（Pipe, CryptoRegistry, Task）
   - 修复 `do` 方法位置错误（CallableDescriptor.do 应在 disable 之后）

2. **使用辅助脚本批量添加 `do`**（逐个文件处理，共处理 28 个文件）：
   - `functional/box.py`, `decorators/overload.py`, `decorators/rself.py`, `decorators/selector.py`
   - `encoding/core.py`, `functional/placeholder_impl.py`, `functional/result.py`
   - `oop/calltype.py`, `oop/extend.py`, `oop/method_extend.py`, `oop/mixer.py`
   - `reactive/core/schedulers.py`, `reactive/core/subject.py`, `reactive/operators/operators.py`
   - `recorder/parser.py`, `security/expression_handler.py`, `security/safe_eval.py`
   - `task/core/worker.py`, `core/dataclass_compat.py`, `crypto/core.py`, `data/vlist.py`
   - `reactive/monitoring/keyboard.py`, `reactive/monitoring/mouse.py`
   - `reactive/monitoring/clipboard.py`, `reactive/monitoring/file_watcher.py`, `reactive/monitoring/folder_watcher.py`
   - `task/core/models.py`, `utils/stuff.py`

3. **扫描验证**：
   - 运行 `dev_tools/scan_do_issues.py` 扫描脚本验证
   - 剩余 2 个误报（`TestSingleton` 和 `Ops`），无需修复

### 产出

- 辅助脚本：`dev_tools/add_do_to_file.py`（批量添加 `do` 到文件，生成 `.new` 供审查）
- 扫描脚本：`dev_tools/scan_do_issues.py`（扫描所有类的 `do` 方法状态）
- 计划文档：`.workbuddy/plan/do_method_fix.md`（已更新完成进度）

### 遵循的规则

- 按 `README_FOR_AI.md` 规则执行
- 排除 Enum 类、dataclass 类、`__slots__` 类
- 逐个文件处理（使用辅助脚本 + 逐文件确认方式）
- 每处理完一个文件，验证并应用 `.new` 文件
